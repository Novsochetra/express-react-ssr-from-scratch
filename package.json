{
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node ./server/dev-server.js",
    "preview": "yarn build:prod && NODE_ENV=production node ./server/dev-server.js",
    "build:client": "npx vite build --outDir dist/client --config vite.config.js --manifest ",
    "build:server": "npx vite build --outDir dist/server --config vite.config.ssr.js",
    "build:prod": "yarn build:client && yarn build:server"
  },
  "dependencies": {
    "express": "^5.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router": "^7.6.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^7.0.3"
  }
}